# *Note* : 
## if the program does not compile, make sure that the @types/node depandancy is installed, //@ts-ignore was used in the enviorment because for some rason typescript could not tell that the types was installed in my enviorment
